package com.example.Park.repository;

import com.example.Park.model.Ride1;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RideRepository extends JpaRepository<Ride1, Integer> {
}
